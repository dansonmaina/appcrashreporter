# Add project specific ProGuard rules here.
# For more details, see https://developer.android.com/build/shrink-code

# Rules specific to building the library in release mode.
# The rules that should propagate to consuming apps live in consumer-rules.pro.
